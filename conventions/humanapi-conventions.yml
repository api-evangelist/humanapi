generated: '2026-07-19'
method: derived
source: openapi/humanapi-admin-openapi.yml + reference.humanapi.co docs
authentication:
  style: bearer-jwt
  token_endpoints:
  - https://auth.humanapi.co/v1/admin/token
  - https://auth.humanapi.co/v1/connect/token
  grant: client_id + client_secret exchanged for a JWT bearer token
  ref: authentication/humanapi-authentication.yml
pagination:
  style: offset
  params:
    offset: index of first record, default 0
    limit: max records per query, default 50
request_tracing:
  field: x-humanapi-request-id
  location: error response body (correlation id echoed on every error)
idempotency:
  supported: false
  note: No idempotency-key header/parameter is documented; not emitting an Idempotency pointer
versioning:
  style: uri-path
  current: v1
error_envelope:
  ref: errors/humanapi-problem-types.yml
  fields:
  - statusCode
  - error
  - message
  - x-humanapi-request-id
events:
  style: webhook notifications
  ref: asyncapi/humanapi-notifications-webhooks.yml
